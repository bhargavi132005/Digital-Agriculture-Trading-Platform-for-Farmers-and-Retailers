server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/product_management_db
    username: root
    password: VishaL@28

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true